'use strict';

describe('Directive: previewAttr', function () {

  // load the directive's module
  beforeEach(module('alexandriaApp'));

  var scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));
});
